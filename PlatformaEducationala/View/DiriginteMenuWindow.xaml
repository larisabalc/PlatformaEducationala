<Window x:Class="PlatformaEducationala.View.DiriginteMenuWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:PlatformaEducationala.ViewModel"
        mc:Ignorable="d"
        Title="DiriginteMenuWindow" Height="300" Width="300">
    <Window.DataContext>
        <vm:DiriginteMenuVM />
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Button Grid.Row="0" Content="MANAGE ABSENTE" Command="{Binding OpenAbsente}"/>
        <Button Grid.Row="1" Content="MANAGE MEDII" Command="{Binding OpenMedii}" />
        <Button Grid.Row="2" Content="LOG OUT" Command="{Binding LogOut}"/>
    </Grid>
</Window>
